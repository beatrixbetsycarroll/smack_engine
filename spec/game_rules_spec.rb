require 'spec_helper'

describe "GameEngine::GAME_RULES" do

  describe "GameEngine::GAME_RULES" do
    it "expects the GAME_RULES to be a hash" do
      expect(GameEngine::GAME_RULES).to be_a Hash
    end
  end

end